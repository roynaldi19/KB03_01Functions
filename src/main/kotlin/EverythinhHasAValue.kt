fun main() {
    val isUnit = println("This is an Expression")
    println(isUnit)
    println()

    val temperature = 10
    val message = "The Water temperature is ${if (temperature > 50) "to warn" else "cold"}"
    println(message)
}